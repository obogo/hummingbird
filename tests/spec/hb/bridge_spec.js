describe('bridge', function () {
    //TODO: need to finish these.
    xit("should bind to angular scope variables that are passed as attributes", function () {

    });

    xit("should emit any event that the hb-scope emits", function() {

    });
});