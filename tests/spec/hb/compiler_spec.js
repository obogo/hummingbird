'use strict';
describe('compiler', function () {

    var module,
        compiler;
    beforeEach(function () {
        module = hb.module('test');
        compiler = hb.compiler(module);
    });


    it("should change the bindMarkings and interpolate with them", function() {

    });
});